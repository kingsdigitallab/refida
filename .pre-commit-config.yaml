exclude: ".git"
default_stages: [commit]
fail_fast: true

repos:
  - repo: local
    hooks:
      - id: flake8
        name: flake8
        entry: flake8
        language: python
        types: [python]
